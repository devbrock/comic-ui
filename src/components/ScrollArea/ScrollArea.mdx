import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./ScrollArea.stories";

<Meta of={Stories} />

# ScrollArea

## Description

**ScrollArea** provides a styled scroll container; use it when content must be scrollable inside a constrained region with consistent scroll UI.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **ScrollArea**
- **ScrollBar**

## Usage

```tsx
import { ScrollArea, ScrollBar } from "@devbrock/comic-ui";

export function Example() {
  return <ScrollArea />;
}
```
