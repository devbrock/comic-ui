import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./Menubar.stories";

<Meta of={Stories} />

# Menubar

## Description

**Menubar** is an application-style menu bar with nested menus; use it for desktop-like UIs where top-level menus group many commands.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **Menubar**
- **MenubarCheckboxItem**
- **MenubarContent**
- **MenubarGroup**
- **MenubarItem**
- **MenubarLabel**
- **MenubarMenu**
- **MenubarPortal**
- **MenubarRadioGroup**
- **MenubarRadioItem**
- **MenubarSeparator**
- **MenubarShortcut**
- **MenubarSub**
- **MenubarSubContent**
- **MenubarSubTrigger**
- **MenubarTrigger**

## Usage

```tsx
import { Menubar, MenubarCheckboxItem, MenubarContent, MenubarGroup, MenubarItem, MenubarLabel, MenubarMenu, MenubarPortal, MenubarRadioGroup, MenubarRadioItem, MenubarSeparator, MenubarShortcut } from "@devbrock/comic-ui";

export function Example() {
  return <Menubar />;
}
```
