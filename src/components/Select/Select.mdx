import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./Select.stories";

<Meta of={Stories} />

# Select

## Description

**Select** is a custom dropdown select (Radix-based); use it for richer select UX than a native `<select>` when you need custom rendering.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **Select**
- **SelectContent**
- **SelectGroup**
- **SelectItem**
- **SelectLabel**
- **SelectScrollDownButton**
- **SelectScrollUpButton**
- **SelectSeparator**
- **SelectTrigger**
- **SelectValue**

## Usage

```tsx
import { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue } from "@devbrock/comic-ui";

export function Example() {
  return <Select />;
}
```
