import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./Empty.stories";

<Meta of={Stories} />

# Empty

## Description

**Empty** is an empty-state panel (icon/title/description/action); use it when a list, table, or search has no results and you need a helpful next step.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **Empty**

## Usage

```tsx
import { Empty } from "@devbrock/comic-ui";

export function Example() {
  return <Empty />;
}
```
