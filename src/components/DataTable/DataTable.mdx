import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./DataTable.stories";

<Meta of={Stories} />

# DataTable

## Description

**DataTable** renders sortable/filterable/paginated tables on top of TanStack Table; use it for real datasets where users need table controls, not just static markup.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **DataTable**

## Usage

```tsx
import { DataTable } from "@devbrock/comic-ui";

export function Example() {
  return <DataTable />;
}
```
