import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./Sonner.stories";

<Meta of={Stories} />

# Sonner

## Description

**Sonner** provides `SonnerToaster` plus `sonnerToast` for lightweight app-wide notifications; use it when you want Sonner-based toasts without framework theme dependencies.
## Variants

### Playground

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories.Playground} />

## Anatomy

- **SonnerToaster**

## Usage

```tsx
import { SonnerToaster } from "@devbrock/comic-ui";

export function Example() {
  return <SonnerToaster />;
}
```
